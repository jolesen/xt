BEGIN;

CREATE TABLE `test_update_db_1`(
    `k` CHAR(64)     NOT NULL COMMENT '',
    `v` VARCHAR(256) NOT NULL COMMENT '',
    KEY(`k`)
)ENGINE=INNODB DEFAULT CHARACTER SET=UTF8 COLLATE=UTF8_GENERAL_CI;

COMMIT;
