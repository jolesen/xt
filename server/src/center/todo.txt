管理所有服务器，
提供获取各种服务器的连接地址等信息，最好不在通过配置表，实现这些数据是动态的

center不主动连接其他服务器，只提供被连接服务器，并记录相关数据。

** 各个业务服务器之间最好是不要直连，而是通过center转发，这样减少耦合

服务器的管理按照类分组，再根据id来管理

不同的服务器间隔上报负载等信息

不同数据可以被不同服务器获取，但是唯一一个服务器

对于不同角色的服务器，可以设置一个节点，比如logic服务器组，
可以设置为一个组，交互从组与组的交互，再到具体的服

也就是在逻辑服务器或网关代码相关的情况下，做到增加服务器就可以增加在线量的设计。

内部消息和外部消息需要分开定义，将header字段放入具体的msg中吧？？？

重新设计消息系统，内部不需要加密，发给client的消息需要加密 ##########################

避免单点故障，center需要多台，多台之间需要进行数据同步，保证一致。
有做法是将多台center同时连接到redis，可以保证数据的统一。

各个逻辑服务器采用按需来获取，也就是需要用到某个连接的时候，判断是否有改服务器的连接，
没有则先获取，然后再连接处理。这样就不必按照流程来，耦合度降低
