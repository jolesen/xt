#include "common/msg/system.h"
#include "common/misc.h"

MSG_SYSTEM(QSysAuthUser)
{
    /*
    RSysAuthUser rsp;
    rsp.authed  = theUsers.Auth(msg.uid, msg.session);
    rsp.uid     = msg.uid;
    rsp.session = msg.session;

    LOG_INFO("uid=%u, session=%u, auth %s", msg.uid, msg.session, rsp.authed ? "success" : "failed");

    SEND_LOGIC(rsp);
    */
}
